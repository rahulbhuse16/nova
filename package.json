{
  "name": "nova-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c green,cyan \"npm run test --prefix nova-server\" \"npm run dev --prefix nova-client\"",
    "backend": "npm run test --prefix nova-server",
    "frontend": "npm run dev --prefix nova-client",
    "build": "npm run build --prefix nova-server && npm run build --prefix nova-client",
    "install-all": "npm install --prefix nova-server && npm install --prefix nova-client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "backend": "^0.0.0",
    "groq-sdk": "^1.5.0"
  }
}
